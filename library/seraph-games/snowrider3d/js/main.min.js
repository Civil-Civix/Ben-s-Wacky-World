/* Local no-ad replacement: keep game pause/resume callbacks working. */
window.gdsdk={AdType:{Rewarded:'rewarded'},preloadAd:()=>Promise.resolve(),showAd:()=>{window.GD_OPTIONS?.onEvent?.({name:'SDK_GAME_START'});return Promise.resolve();}};
window.GD_OPTIONS?.onEvent?.({name:'SDK_READY'});
